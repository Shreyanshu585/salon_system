

server:
  port: 8066

spring:
  application:
    name: service-Offering

  datasource:
    url: jdbc:mysql://localhost:3306/servicesdb
    username: root
    password: Shree1@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address : true

  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8070/eureka/